GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com

# Gradle version to pre-download into the image (default: 9.3.1)
GRADLE_VERSION=9.3.1

# Ports exposed by dev-runner (default: 8080, 8081)
# NOTE: Port mappings are set at container creation time. If you change these
# after dev-runner is already running, remove the old container first:
#   docker rm -f dev-runner
DEV_PORT_1=8080
DEV_PORT_2=8081

# Ports exposed by gradle-runner (default: 8082, 8083)
# NOTE: Port mappings are set at container creation time. If you change these
# after gradle-runner is already running, remove the old container first:
#   docker rm -f gradle-runner
GRADLE_PORT_1=8082
GRADLE_PORT_2=8083
